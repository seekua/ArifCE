fetch('/api/overview').then(r=>r.json()).then(o=>{const events=o.events||[];const card=document.createElement('div');card.className='col-12';card.innerHTML='<div class="card"><div class="card-header"><h2 class="card-title">Agent activity timeline</h2><div class="text-secondary small">Who acted, when, and which canonical record changed</div></div><div class="table-responsive"><table class="table table-vcenter"><thead><tr><th>When</th><th>Agent</th><th>Action</th><th>Record</th></tr></thead><tbody>'+(events.length?events.slice(0,20).map(e=>'<tr><td class="text-secondary small">'+new Date(e.occurredAtUtc).toLocaleString()+'</td><td><span class="badge bg-blue-lt">'+(e.actor||'repository')+'</span></td><td>'+e.type+'</td><td><code>'+e.entityId+'</code></td></tr>').join(''):'<tr><td colspan="4" class="text-secondary">No agent activity recorded yet.</td></tr>')+'</tbody></table></div></div>';document.querySelector('#records')?.before(card)}).catch(()=>{});
