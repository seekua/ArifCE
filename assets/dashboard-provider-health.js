fetch('/api/llm/providers').then(r=>r.json()).then(items=>{const card=document.createElement('div');card.className='col-12';card.innerHTML='<div class="card"><div class="card-header"><h2 class="card-title">Provider health</h2><div class="text-secondary small">Connection tests run locally</div></div><div class="table-responsive"><table class="table table-vcenter"><thead><tr><th>Profile</th><th>Provider</th><th>Model</th><th>Runtime</th><th>Status</th><th></th></tr></thead><tbody>'+(items.length?items.map(x=>'<tr><td><code>'+x.id+'</code></td><td>'+x.provider+'</td><td>'+x.model+'</td><td>'+x.runtime+'</td><td id="health-'+x.id+'">'+(x.enabled?'Enabled':'Disabled')+'</td><td><button class="btn btn-sm" data-provider="'+x.id+'">Test connection</button></td></tr>').join(''):'<tr><td colspan="6" class="text-secondary">No provider profiles configured.</td></tr>')+'</tbody></table></div></div>';document.querySelector('#records')?.before(card);card.querySelectorAll('button[data-provider]').forEach(button=>button.onclick=async()=>{button.disabled=true;const id=button.dataset.provider;const response=await fetch('/api/llm/providers/'+encodeURIComponent(id)+'/test',{method:'POST'});const result=await response.json();const cell=card.querySelector('#health-'+CSS.escape(id));if(cell)cell.textContent=result.success?'Connected':'Failed: '+result.message;button.disabled=false})}).catch(()=>{});
