# Provenance

Provenance answers where a piece of project knowledge came from: user confirmation, decision, Git state, test/build evidence, checkpoint, session, raw transcript, or inference.

ArifCE links IDs across tasks, decisions, attempts, claims, evidence, findings, reviews, checkpoints, and refactors. `arifce why` reports known indexed links and says explicitly when rationale is unknown.
