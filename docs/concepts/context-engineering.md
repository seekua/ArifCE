# Context Engineering

Context engineering selects the smallest useful projection of project intelligence for a task. The goal is not to preserve every token in every prompt; it is to preserve broadly and retrieve selectively.

V0.1 uses explainable lexical retrieval and stable metadata rather than embeddings. Raw transcripts are excluded. Effectiveness must be benchmarked using task correctness, irrelevant reads, latency, and context cost.
