# OpenCode

`opencode.json` points OpenCode to `AGENTS.md`, `.arifce/PROTOCOL.md`, and `.arifce/CURRENT.md`. The adapter contains routing instructions only.

OpenCode should retrieve task-specific context, preserve canonical state formats, avoid raw transcript ingestion, and attach evidence to claims rather than reporting unsupported completion.
