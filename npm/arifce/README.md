# @arifce/cli

Thin local launcher for the ArifCE .NET tool. It does not send project data to npm or a hosted service.

```bash
dotnet tool install --global ArifCE.Cli
npx @arifce/cli status
```

Native self-contained binaries remain tracked separately in the NativeAOT distribution plan.
